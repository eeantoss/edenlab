# 2026-03-18

## Star-Office-UI: Polygon 链上数据面板
- 在 Star-Office-UI 后端 (app.py) 新增 `/polygon-data` API，通过 QuickNode Polygon RPC 获取区块高度和 Gas 价格，30 秒缓存
- 在前端 index.html 新增像素风 Polygon 面板，每 30 秒自动刷新
- RPC URL 改为环境变量 `POLYGON_RPC_URL` 读取，避免公开仓库泄露
- 同步修改到 EdenLab 项目：新增 Next.js API route (`app/api/polygon-data/route.ts`) + 前端面板
- 两个仓库均已推送到 GitHub，Vercel 自动部署
- 待办：Vercel 需配置 `POLYGON_RPC_URL` 环境变量

## 钱包地址
- 用户提供的 Polygon 钱包地址: `0x0f9d2eb02d7e5062a85925436b9734459ffe84a`（41 位，疑似少一位，余额为 0）

## 待处理
- Polygon 面板尺寸太小，需要放大并调整位置
- 昨日小记面板内容未更新（需排查）
